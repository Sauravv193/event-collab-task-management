# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/event_task_db
spring.datasource.username=postgres
spring.datasource.password=saurav

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=yourSuperSecretKeyChangeThisInProductionWithAtLeast64CharactersLong
jwt.expiration.ms=86400000

# Server Configuration
server.port=8080

# Logging
logging.level.aura.event_based_task=DEBUG
logging.level.org.springframework.security=DEBUG